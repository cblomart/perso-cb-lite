
# Coinbase Advanced Trade API Configuration
# You need to create an API key in your Coinbase Advanced Trade account
# Go to Settings > API > Create New API Key
# IMPORTANT: Select ECDSA as signature algorithm (NOT Ed25519)
# Use the id as COINBASE_API_KEY and the PEM private key as COINBASE_API_SECRET
COINBASE_API_KEY=your_api_key_id_here
# For the private key, you can use either format:
# Option 1: Single line with escaped newlines (recommended for .env files)
COINBASE_API_SECRET=-----BEGIN EC PRIVATE KEY-----\nMIGHAgEAMBMGByqGSM49AgEGCCqGSM49AwEHBG0wawIBAQQg...\n-----END EC PRIVATE KEY-----
# Option 2: Just the base64 key content (without PEM headers)
# COINBASE_API_SECRET=MIGHAgEAMBMGByqGSM49AgEGCCqGSM49AwEHBG0wawIBAQQg...

# Trading Configuration
# Base currency (e.g., BTC, ETH, SOL)
TRADING_BASE_CURRENCY=BTC
# Quote currency (e.g., USDC, USD, EUR)
TRADING_QUOTE_CURRENCY=USDC
# Trading pair (auto-generated from base/quote, but can be overridden)
# TRADING_PAIR=BTC-USDC

# Security Configuration
# Generate a random access key for API protection (UUID format)
# Leave empty to auto-generate on startup
API_ACCESS_KEY=
# Rate limiting: requests per minute per IP
RATE_LIMIT_REQUESTS_PER_MINUTE=60
# IP whitelist (comma-separated, leave empty to allow all)
# ALLOWED_IPS=192.168.1.100,10.0.0.50
# Enable/disable security features
ENABLE_RATE_LIMITING=true
ENABLE_IP_WHITELIST=false
ENABLE_ACCESS_KEY_AUTH=true

# Server Configuration
PORT=8080
ENVIRONMENT=development

# Logging Configuration
# If not set, automatically uses:
# - WARN level in production (less verbose)
# - INFO level in development (more verbose)
# Available levels: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO 